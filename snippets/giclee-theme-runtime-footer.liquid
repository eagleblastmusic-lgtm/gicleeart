  <script>
    (function() {
      var handle = 'twoje-zdjecie-jako-wydruk-giclee-na-papierze-fine-art-w-drewnianej-ramie';
      var productPath = '/products/' + handle;
      function fixWlasnaFotografiaLinks() {
        document.querySelectorAll('a[href*="/pages/fotografia-obraz"]').forEach(function(a) {
          a.setAttribute('href', productPath);
        });
      }
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', fixWlasnaFotografiaLinks);
      } else {
        fixWlasnaFotografiaLinks();
      }
    })();
  </script>
  {% render 'giclee-site-notice' %}
  {% if template.suffix == 'faq' %}
  <script>
    /* FAQ a11y + wycinek 1. akordeonu (fade-in, prawy dolny róg viewportu) */
    (function () {
      var FIRST_CUTOUT_SRC = {{ 'faq-accordion-first-cutout.png' | asset_url | json }};

      function ensureFirstAccordionCutout() {
        var existing = document.querySelector('.faq-accordion-cutout');
        if (existing) return existing;

        var img = document.createElement('img');
        img.className = 'faq-accordion-cutout';
        img.src = FIRST_CUTOUT_SRC;
        img.alt = '';
        img.setAttribute('aria-hidden', 'true');
        img.decoding = 'async';
        img.loading = 'eager';
        document.body.appendChild(img);
        return img;
      }

      function syncFirstAccordionCutout(details) {
        var cutout = ensureFirstAccordionCutout();
        if (!cutout) return;
        if (details.open) {
          cutout.classList.add('is-visible');
        } else {
          cutout.classList.remove('is-visible');
        }
      }

      function bindFirstAccordionCutout() {
        var first = document.querySelector('.accordion accordion-custom');
        if (!first) return;
        var details = first.querySelector('details');
        if (!details) return;

        ensureFirstAccordionCutout();
        syncFirstAccordionCutout(details);
        details.addEventListener('toggle', function () {
          syncFirstAccordionCutout(details);
        });
      }

      function enhanceFaqA11y() {
        var items = document.querySelectorAll('.accordion accordion-custom');
        items.forEach(function (item, i) {
          var details = item.querySelector('details');
          if (!details) return;
          var summary = details.querySelector('summary');
          var content = details.querySelector('.details-content');
          if (!summary) return;

          if (content) {
            if (!content.id) content.id = 'faq-answer-' + (i + 1);
            summary.setAttribute('aria-controls', content.id);
          }
          summary.setAttribute('aria-expanded', details.open ? 'true' : 'false');

          details.addEventListener('toggle', function () {
            summary.setAttribute('aria-expanded', details.open ? 'true' : 'false');
          });
        });
        bindFirstAccordionCutout();
      }
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', enhanceFaqA11y);
      } else {
        enhanceFaqA11y();
      }
    })();
  </script>
  {% endif %}
