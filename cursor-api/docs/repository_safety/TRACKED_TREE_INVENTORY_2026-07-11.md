# GicleeApp tracked-tree inventory — 2026-07-11

Repository: `eagleblastmusic-lgtm/gicleeapp`

Branch: `gpt-work/repo-safety-foundation`

Discovery head: `7df7955638165c23420bca3214020fa91e683087`

## Safety status

This document records repository-relative paths and aggregate counts only. It does not reproduce customer data, credentials, invoice values or file contents.

The inventory was generated by GitHub Actions in read-only discovery mode:

- no `--copy`;
- no source modification;
- no destination creation;
- no `git rm`;
- no staged deletions;
- no merge or deploy.

## Audited tree

- tracked files: **1417**
- source files: **1352**
- examples: **4**
- prohibited classifications/findings: **65 findings across 62 unique paths**
- unknown/unclassified paths: **0**
- content-secret findings after calibration: **0**

Three paths intentionally produce two findings:

- `10.0.0` — generated artifact plus the dedicated accidental-artifact rule;
- `Komponenty/dokumentysprzedazy/dane/invoice_settings.json` — private data plus detected PII fields;
- `Komponenty/dokumentysprzedazy/dane/exports/sales_2026_06.csv` — private data plus detected PII columns.

## Prohibited tracked paths by class

### Backups — 36 paths

| Component | Count | Pattern |
|---|---:|---|
| FAQ | 6 | `Komponenty/faq/data/backups/**` |
| Kontakt | 2 | `Komponenty/kontakt/data/backups/**` |
| Losuj Obraz | 19 | `Komponenty/losujobraz/data/backups/**` |
| Submenu katalog | 9 | `Komponenty/submenukatalog/data/backups/**` |

Migration bucket: `%LOCALAPPDATA%\GicleeArt\GicleeApp\backups\`.

### Runtime state — 17 paths

- 16 Home variant files:
  - `Komponenty/stronaglowna/data/variants/home1..home8/index.json`
  - `Komponenty/stronaglowna/data/variants/home1..home8/settings.json`
- launcher layout:
  - `giclee_app/data/launcher_layout.json`

Migration buckets:

- Home variants → `data`;
- launcher layout → `config`.

The files remain tracked until the application has validated first-run and new-write-path support.

### Private sales/invoice data — 4 paths

- `Komponenty/dokumentysprzedazy/dane/exports/sales_2026_06.csv`
- `Komponenty/dokumentysprzedazy/dane/invoice_settings.json`
- `Komponenty/dokumentysprzedazy/dane/invoices.json`
- `Komponenty/dokumentysprzedazy/dane/orders_sync_state.json`

Migration bucket: `%LOCALAPPDATA%\GicleeArt\GicleeApp\data\`.

The audit reports PII metadata only; this checkpoint does not reproduce values from those files.

### Cache — 2 paths

- `Komponenty/tldobio/data/collections.json`
- `Komponenty/tldobio/data/vincent-van-gogh-bio-test.jpg`

Migration bucket: `data` for preservation before cleanup. The application may later regenerate these files after cache-path migration.

### Local credential/config file — 1 path

- `.npmrc`

Migration bucket: `%APPDATA%\GicleeArt\GicleeApp\config\`.

No credential content is included in this inventory.

### Generated artifacts — 2 paths

- `10.0.0`
- `_dup_result.txt`

These paths do not require data migration. They are future candidates for removal from tracking after local validation. The current branch does not delete them.

## Copy-free migration plan

GitHub Actions generated a dry-run plan with:

- total migration items: **60**
- status: **all `planned`**
- blocked: **false**
- backup bucket: **36**
- data bucket: **22**
- config bucket: **2**
- generated paths without migration: **2**

Each item contains a source SHA-256 and a proposed destination. The CI targets are isolated temporary directories and do not prove that the user's real Windows destinations are conflict-free.

## Audit calibration record

| Step | Blocker findings | Result |
|---|---:|---|
| Initial tracked-tree scan | 107 | Broad named-secret matching produced false positives |
| Literal-only secret matching | 74 | Identifier/function/environment lookups removed |
| Quoted UI context detection | 71 | API-key labels in interface text removed |
| Verified source/generated classification | 64 | Seven legitimate source paths classified; `_dup_result.txt` remains generated |
| Sales export migration classification | 65 | Export correctly gains `PRIVATE_USER_DATA` in addition to PII finding |

The final increase from 64 to 65 is intentional and does not add a path: the existing sales CSV now has a migration classification instead of only a content finding.

## Local validation still required

The following evidence cannot be produced from GitHub:

- untracked runtime/private files in `C:\Strona\pusty\cursor-api`;
- conflicts in the user's actual LocalAppData/RoamingAppData destinations;
- SHA verification after a real copy;
- first-run behavior after source-tree runtime files are absent;
- manual Push GicleeApp dry-run against `C:\Strona\_gicleeapp_staging`.

Until that evidence is reviewed:

- do not use `git rm` or `git rm --cached`;
- do not enable stale-file deletion;
- do not import this package into `gicleeart`;
- do not merge PR #4;
- do not deploy Shopify.
